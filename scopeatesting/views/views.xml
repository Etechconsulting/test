<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="scopeatesting.list">
      <field name="name">scopeatesting list</field>
      <field name="model">scopeatesting.scopeatesting</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="scopeatesting.action_window">
      <field name="name">scopeatesting window</field>
      <field name="res_model">scopeatesting.scopeatesting</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="scopeatesting.action_server">
      <field name="name">scopeatesting server</field>
      <field name="model_id" ref="model_scopeatesting_scopeatesting"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="scopeatesting" id="scopeatesting.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="scopeatesting.menu_1" parent="scopeatesting.menu_root"/>
    <menuitem name="Menu 2" id="scopeatesting.menu_2" parent="scopeatesting.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="scopeatesting.menu_1_list" parent="scopeatesting.menu_1"
              action="scopeatesting.action_window"/>
    <menuitem name="Server to list" id="scopeatesting" parent="scopeatesting.menu_2"
              action="scopeatesting.action_server"/>
    -->
  </data>
</odoo>